'''Sets the defaults. Appart form configuration, independant of imports. This
could be considdered as a configuration function.'''

def set_defaults_for_missing_values(multi_dict):

    return ("/buy_tickets/charge#processed"
        , "Tickets"
        , 'Nothing to process')
